# miles-sandbox -- pinned dependencies
#
# Pin status:
#   - jax, jaxlib, numpy, scipy, s2fft pinned to versions OBSERVED in a
#     working Colab TPU v6e session (2026-05-08 s2fft install smoke test).
#   - equinox unpinned; the resolved version is recorded in the test_04
#     _meta block on first Colab install and can be pinned from there.
#   - Test 05 deps (tensorflow + xarray/zarr/netCDF4/h5netcdf) are
#     COMMENTED OUT below -- they conflicted with jax 0.7.2 on Colab
#     (2026-05-12): tensorflow 2.18.0 pre-dates numpy 2.x and pulls
#     incompatible numpy/protobuf/ml-dtypes. Restore + re-pin to
#     verified versions when Test 05 implementation begins.
#   - pyyaml left as a scaffold pin; harmless with the current stack.
#
# Per CLAUDE.md rule 13, any future change to jax / jaxlib / libtpu is a
# human decision -- TPU / JAX / libtpu version interactions are real, and
# a silent bump can mask or introduce numerical regressions on TPU MXUs.
#
# Install command for Colab (the -f flag is required for libtpu wheels):
#   pip install -q -r requirements.txt \
#       -f https://storage.googleapis.com/jax-releases/libtpu_releases.html

# --- JAX / TPU stack -------------------------------------------------------
# Versions observed working in Colab TPU v6e session, 2026-05-08.
jax==0.7.2
jaxlib==0.7.2
# libtpu pin is paired with jaxlib; resolved automatically via the -f index
# in the install command above. Pin explicitly here once the working
# (jax, jaxlib, libtpu) triple is locked in by a successful Test 01 run.
# libtpu==0.0.<TBD>

# --- Test 01: spherical harmonics on TPU -----------------------------------
# Version observed working in Colab TPU v6e session, 2026-05-08.
s2fft==1.4.0

# --- Test 04: stand-in WxFormer for scan/compile-cost measurement ----------
# Equinox is the JAX-native module framework we picked over Flax NNX for the
# stand-in (one-off test, not infrastructure). Pin gets locked after first
# Colab install reports the resolved version (recorded in test_04 _meta
# under model.equinox_version). Per CLAUDE.md rule 13, do not silently bump.
equinox

# --- Test 05: host data pipeline (COMMENTED OUT) ---------------------------
# Disabled 2026-05-12 due to pip resolution conflict between tensorflow
# 2.18.0 (pre-numpy-2.x; pulls older numpy/protobuf/ml-dtypes) and
# jax/jaxlib 0.7.2 (post-numpy-2.x). Uncomment and re-pin to verified
# versions when Test 05 implementation begins. tensorflow is CPU-only
# on the Colab TPU host (TPU is fed via tf.data, not TF execution);
# install the standard wheel, not tensorflow-tpu.
# tensorflow==2.18.0
# xarray==2024.10.0
# zarr==2.18.3
# netCDF4==1.7.2
# h5netcdf==1.4.1

# --- Numerics + utilities --------------------------------------------------
# numpy and scipy versions observed in Colab TPU v6e session, 2026-05-08.
# scipy is a transitive dep of s2fft; pinned explicitly for reproducibility.
numpy==2.0.2
scipy==1.16.3
pyyaml==6.0.2      # SCAFFOLD pin; YAML configs follow CLAUDE.md rule 11.

# --- Inspection ------------------------------------------------------------
# HLO extraction and JSON results emission use only the standard library;
# no extra deps required.
